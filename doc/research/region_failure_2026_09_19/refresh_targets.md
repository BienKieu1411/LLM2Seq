# Refresh targets

- Add the two runs' `resolved_config.yaml`, train/validation curves and checkpoint hashes if they become available on the server.
- Add per-ID predictions for direct, static, hidden-region, Q-space and new prior; calculate paired deltas and bootstrap intervals.
- Record region-on/off validation CE/ROUGE, residual norm and gate by decoder layer.
- Update this report only after candidate validation; do not revise the locked paper from unverified architecture hypotheses.

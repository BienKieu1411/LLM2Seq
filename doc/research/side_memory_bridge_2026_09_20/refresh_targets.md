# Refresh targets

- Recheck LongT5, Landmark Attention, GTCA, HMT, and recent side-memory papers for newer summarization-specific ablations.
- Record whether the next bridge preserves `(K0,V0)` byte-for-byte and whether copy still reads only `H0`.
- Track validation ROUGE deltas and side-bank intervention deltas separately.
- Track gate saturation, side/native context norm ratio, copy mass, and throughput/VRAM.
- Reassess the CE-only constraint before considering any auxiliary compression or evidence loss.


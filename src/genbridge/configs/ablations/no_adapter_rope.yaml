_base_: ../base.yaml

experiment:
  name: genbridge_without_adapter_rope
  output_dir: runs/genbridge/no_adapter_rope

bridge:
  # Keep the same attention/FFN parameters and remove only the explicit
  # relative-position rotation from token and evidence-unit attention.
  use_adapter_rope: false

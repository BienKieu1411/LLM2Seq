_base_: ../base.yaml

experiment:
  name: genbridge_concat_memory
  output_dir: runs/genbridge/concat_memory

decoder:
  # Same bridge and objectives as the proposed model, but a single attention
  # softmax over [plan; token] memory. This isolates the dual-memory mechanism.
  memory_attention: concat

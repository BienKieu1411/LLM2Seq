_base_: ../base.yaml

experiment:
  name: causal_encoder_decoder
  output_dir: runs/genbridge/causal_ed

bridge:
  mode: causal
  use_salience_loss: false
  use_plan_alignment: false

decoder:
  memory_attention: concat

objectives:
  salience_weight: 0.0
  plan_alignment_weight: 0.0
  plan_diversity_weight: 0.0

_base_: ../base.yaml

experiment:
  name: causal_encoder_decoder
  output_dir: runs/evibridge/causal_ed

bridge:
  mode: causal
  num_evidence_slots: 0
  use_evidence_loss: false

objectives:
  evidence_weight: 0.0
  diversity_weight: 0.0

_base_: ../base.yaml

experiment:
  name: lamate_style_postencoder
  output_dir: runs/evibridge/lamate_style

model:
  layer_fusion:
    enabled: true
    indices: [-1, -5, -9, -13]

bridge:
  mode: lamate
  num_evidence_slots: 0
  use_evidence_loss: false

objectives:
  evidence_weight: 0.0
  diversity_weight: 0.0

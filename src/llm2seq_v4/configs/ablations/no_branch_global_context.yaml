_base_: ../qwen3_0_6b_hiroute.yaml

experiment:
  name: llm2seq_v4_ablation_no_branch_global_context
  output_dir: runs/llm2seq_v4/ablations/no_branch_global_context

adapter:
  # Keep all three banks and the adaptive output router; remove only the
  # bidirectional summary-context injection into lexical/semantic memories.
  branch_global_context: false

_base_: ../tasks/wikilingua.yaml

experiment:
  name: eviseq_nemotron_1b_to_qwen3_0_6b
  output_dir: runs/eviseq/encoders/nemotron_1b

model:
  encoder_name: nvidia/Nemotron-3-Embed-1B-BF16
  encoder_hidden_size: 2048
  encoder_attn_implementation: sdpa
  encoder_trust_remote_code: false
  encoder_revision: a5e0f804b9e90a1ca6784ecbf6e41595774fc834
  dtype: bfloat16

native_attention:
  _replace_: true
  backend: pretrained_native
  variant: pretrained

objectives:
  contrastive_pooling: mean

data:
  source_prefix: "passage: "

reporting:
  parameter_efficiency_claim_eligible: false
  reason: "1.14B encoder plus decoder/cross-attention is above T5Gemma2-1B-1B's approximately 1.7B footprint"

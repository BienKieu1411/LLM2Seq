_base_: ../tasks/wikilingua.yaml

experiment:
  name: eviseq_pplx_0_6b_to_qwen3_0_6b
  output_dir: runs/eviseq/encoders/pplx_0_6b

model:
  encoder_name: perplexity-ai/pplx-embed-v1-0.6b
  encoder_hidden_size: 1024
  encoder_attn_implementation: sdpa
  encoder_trust_remote_code: true

native_attention:
  _replace_: true
  backend: pretrained_native
  variant: pretrained

objectives:
  contrastive_pooling: mean

data:
  source_prefix: ""

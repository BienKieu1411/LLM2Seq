_base_: pplx_pubmed_pceb.yaml

# One-axis PCEB sensitivity: allow one additional complementary source
# sentence for a long PubMed abstract.  All bridge, loss, schedule, prompt,
# and decoding settings remain identical to the locked PCEB main recipe.
experiment:
  name: eviseq_pubmed_pceb_pplx_0_6b_pos4
  output_dir: runs/eviseq/pubmed_pceb_pplx_0_6b_pos4

data:
  oracle_max_units: 4

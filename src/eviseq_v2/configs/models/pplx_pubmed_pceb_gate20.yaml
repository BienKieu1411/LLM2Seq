_base_: pplx_pubmed_pceb.yaml

# One-axis PCEB sensitivity: start copied source cross-attention at a larger
# residual contribution.  It tests source uptake without altering evidence
# labels, contrastive pressure, bridge projection, or decoding.
experiment:
  name: eviseq_pubmed_pceb_pplx_0_6b_gate20
  output_dir: runs/eviseq/pubmed_pceb_pplx_0_6b_gate20

decoder:
  cross_gate_init: 0.20

_base_: afmr_base.yaml

experiment:
  name: eviseq_update_v51_semantic_only_v2
  output_dir: runs/eviseq_update_v5/semantic_only_v2

decoder:
  grounded_copy:
    enabled: true
    readout_mode: legacy_v2
    semantic_read:
      enabled: true
      key_source: H0
      value_source: H0
      semantic_prior_scale: 1.0
      max_relative_rms: 0.10
      cap_mode: legacy_v2
      inner_gate: true
      gate_init: 0.05
      output_init: zero

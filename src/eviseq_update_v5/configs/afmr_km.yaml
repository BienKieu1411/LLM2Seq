_base_: afmr_base.yaml

experiment:
  name: eviseq_update_v51_km
  output_dir: runs/eviseq_update_v5/km

decoder:
  grounded_copy:
    semantic_read:
      key_source: M
      semantic_prior_scale: 0.0

_base_: base.yaml

experiment:
  name: warmup_causal_decoder
  output_dir: runs/adabimask/warmup_causal

model:
  use_lora: false
  train_base: true

mask:
  mode: causal

training:
  epochs: 10
  interface_warmup_epochs: 2
  stop_after_interface: true

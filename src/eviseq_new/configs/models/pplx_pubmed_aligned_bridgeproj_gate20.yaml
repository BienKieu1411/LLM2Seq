_base_: pplx_pubmed_aligned_bridgeproj.yaml

# Follow-up architecture run: preserve the identity-initialized PPLX->Qwen
# bridge correction and give the copied decoder cross-attention a moderately
# stronger source residual from the first update.  Greedy inference and the
# one encoder -> bridge -> decoder graph are unchanged.
experiment:
  name: eviseq_pubmed_pplx_aligned_bridgeproj_gate20_0_6b_to_qwen3_0_6b
  output_dir: runs/eviseq/pubmed_pplx_aligned_bridgeproj_gate20_0_6b

decoder:
  cross_gate_init: 0.20

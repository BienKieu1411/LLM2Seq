_base_: afmr_base.yaml

experiment:
  name: afmr_core_legacy_semantic
  output_dir: runs/afmr_core/legacy_semantic

decoder:
  grounded_copy:
    enabled: true
    readout_mode: legacy_copy_mixture
    semantic_read:
      enabled: true
      key_source: H0
      value_source: H0
      semantic_prior_scale: 1.0
      max_relative_rms: 0.10
      cap_mode: legacy
      inner_gate: true
      gate_init: 0.05
      output_init: zero

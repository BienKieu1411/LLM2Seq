_base_: ../qwen3_0_6b_hiroute.yaml

experiment:
  name: llm2seq_v3_ablation_static_output_routing
  output_dir: runs/llm2seq_v3/ablations/static_output_routing

decoder:
  # Keep independent bank attention and post-attention fusion, but use only
  # the learned per-depth prior rather than a token-conditioned residual.
  query_adaptive_routing: false

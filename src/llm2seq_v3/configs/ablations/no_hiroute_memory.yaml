_base_: ../qwen3_0_6b_hiroute.yaml

experiment:
  name: llm2seq_v3_ablation_no_hiroute_memory
  output_dir: runs/llm2seq_v3/ablations/no_hiroute_memory

adapter:
  # Preserve the main model's 8 bidirectional layers and sentence context;
  # remove only the lexical/semantic banks and output router.
  depth_routed_memory: false

decoder:
  memory_bank_count: 1
  query_adaptive_routing: false

objectives:
  routing_balance_weight: 0.0

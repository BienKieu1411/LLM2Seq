# Data

LLM2Seq-v3 reads its WikiLingua JSONL splits from this directory:
`data/wikilingua/`. The v3 configuration records paths and example counts for
the three files used by the paper comparison.

The v3 training and evaluation flows are self-contained and do not depend on
the sibling `llm2seq_v2/` directory.
